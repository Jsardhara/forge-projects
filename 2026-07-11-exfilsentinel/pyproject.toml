[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "exfilsentinel"
version = "0.1.0"
description = "AI model-access exfiltration detector and output-provenance watermarking for trade-secret defense"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/exfilsentinel"]
