"""AI Agent Security Sandbox — Local proxy + audit toolkit for AI agent safety.

Provides:
- API key vault with scoped permissions (env-based, never in source)
- Network egress control via allowlist proxy
- Filesystem access monitoring
- Audit logging of all agent actions
- One-click kill switch
"""

__version__ = "0.1.0"
