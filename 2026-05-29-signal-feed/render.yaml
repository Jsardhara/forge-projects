# Render deployment configuration
# https://render.com/docs/deploy-fast-api

build_command = "pip install -r requirements.txt"
start_command = "uvicorn signal_feed.api:app --host 0.0.0.0 --port $PORT"
